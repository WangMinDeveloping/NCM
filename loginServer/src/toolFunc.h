#ifndef toolFunc_h
#define toolFunc_h
void invertFunction(void * vData,int length);//倒置函数声明
void invertWord(unsigned char *vData);//倒置传进来的指针所指向的空间中的内容。
unsigned short invertWord(unsigned short Data);//返回2字节倒置值。
#endif
